import { Screen } from '@/components/layout';
import { Text } from '@/components/ui';

export function PrivacySettingsScreen() {
  return (
    <Screen>
      <Text variant="title">Privacy</Text>
      <Text muted>Settings module — implementation pending</Text>
    </Screen>
  );
}
