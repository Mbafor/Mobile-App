import { Screen } from '@/components/layout';
import { Text } from '@/components/ui';

export function OpportunitiesListScreen() {
  return (
    <Screen>
      <Text variant="title">Opportunities</Text>
      <Text muted>Opportunities module — implementation pending</Text>
    </Screen>
  );
}
