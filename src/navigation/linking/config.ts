export const linkingPrefixes = ['Voila://'];

/**
 * Expo Router linking config — merge into expo-router when deep links are enabled.
 */
export const linkingConfig = {
  prefixes: linkingPrefixes,
};
